services:
  app:
    build: .
    user: "1000"
    env_file: .env
    volumes:
      - app_db:/app/db
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  app_db:
